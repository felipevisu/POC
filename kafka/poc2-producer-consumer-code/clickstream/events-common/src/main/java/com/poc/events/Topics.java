package com.poc.events;

/** Topic names, shared so producer and consumer can't drift apart. */
public final class Topics {
    public static final String PAGE_VIEWS = "page-views";

    private Topics() {
    }
}
