# Java Streams

Practicing the Stream API over a `List<Person>`: filter, map, sorted, collect, `mapToInt().average()`, `count`, and `Collectors.joining`. Last example does the same filter+sort with a classic `for` loop for comparison.

## Run

```bash
mvn compile exec:java -Dexec.mainClass=org.example.Main
```
