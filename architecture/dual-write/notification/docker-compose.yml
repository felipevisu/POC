services:
  notification:
    build: .
    environment:
      DATABASE_URL: postgres://postgres:postgres@postgres:5432/notifications
      RABBITMQ_URL: amqp://rabbit:rabbit@rabbitmq:5672
      DELAY_MS: "100"
    ports:
      - "3001:3000"
    depends_on:
      postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy

  # the queue lives here so it stays up while the notification service is down
  rabbitmq:
    image: rabbitmq:4-management
    environment:
      # not guest: guest is only allowed from localhost, the version APIs connect through the host
      RABBITMQ_DEFAULT_USER: rabbit
      RABBITMQ_DEFAULT_PASS: rabbit
    ports:
      - "5672:5672"
      - "15672:15672"
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
      interval: 5s
      timeout: 10s
      retries: 12

  postgres:
    image: postgres:17
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: notifications
    ports:
      - "5433:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d notifications"]
      interval: 2s
      timeout: 3s
      retries: 15
